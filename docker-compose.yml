services:
  streamlit:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ah-premium-lab
    ports:
      - "8501:8501"
    environment:
      PYTHONPATH: /app/src
      STREAMLIT_SERVER_HEADLESS: "true"
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs
